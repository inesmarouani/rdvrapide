# Copie ce fichier en ".env" dans backend/ pour un développement local
# sans Docker (avec un PostgreSQL local ou distant).

DATABASE_URL=postgresql+psycopg://rdvrapide:rdvrapide@localhost:5432/rdvrapide
ANTHROPIC_API_KEY=
ENVIRONMENT=development
APP_PASSWORD=dev-local
SECRET_KEY=dev-secret-key-not-for-production
